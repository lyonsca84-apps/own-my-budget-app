{
  "name": "@own-my-budget/api",
  "version": "0.1.0",
  "private": true,
  "description": "Typed Supabase data-access layer — generated database types plus a thin client factory. UI code never imports @supabase/supabase-js directly.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
