import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function MoneyScreen() {
  return (
    <PlaceholderScreen
      title="Debt payoff and savings goals"
      message="Snowball vs. avalanche comparisons, credit utilization, and savings goal tracking (including the 52-week challenge) land in Phase 5."
    />
  );
}
