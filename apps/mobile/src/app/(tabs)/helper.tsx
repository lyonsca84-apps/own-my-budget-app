import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function HelperScreen() {
  return (
    <PlaceholderScreen
      title="Budget Buddy is on the way"
      message="Your AI assistant for bills, receipts, and pantry scans arrives in Phase 6, once the server-side Anthropic API integration and usage metering are in place."
    />
  );
}
