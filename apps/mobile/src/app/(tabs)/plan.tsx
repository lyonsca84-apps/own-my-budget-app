import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function PlanScreen() {
  return (
    <PlaceholderScreen
      title="Your budget is coming together"
      message="Paycheck assignment, categories, and planned-vs-actual tracking land in Phase 4, once the real budgeting math is wired up in packages/core."
    />
  );
}
